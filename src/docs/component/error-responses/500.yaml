description: |
  서버쪽에서 에러가 발생했을 때의 레스폰스
content:
  application/json:
    schema:
      title: System Error
      type: object
      properties:
        fields:
          type: object
          description: 'hogogh'
        global:
          type: array
          description: '에러를 나타내는 배열'
          items:
            type: object
            properties:
              code:
                type: string
                description: '코드'
                example: AMS90002E
              message:
                type: string
                description: '메세지'
                example: 부정한 리퀘스트입니다.
