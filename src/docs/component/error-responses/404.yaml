description: |
  페이지를 찾을 수 없는 경우의 레스폰스
content:
  application/json:
    schema:
      title: NotFound
      type: object
      properties:
        fields:
          type: object
          description: 'hogogh'
        global:
          type: array
          description: '에러를 나타내는 배열'
          items:
            type: object
            properties:
              message:
                type: string
                description: '메세지'
                example: 페이지를 찾을 수 없습니다.
